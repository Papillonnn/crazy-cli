'use strict';

module.exports = init;

function init(projectName, cmdObj) {
    console.log('init');
    // console.log(projectName);
    // console.log(cmdObj);
    // console.log(process.env.CLI_TARGET_PATH);
}
